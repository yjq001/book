name: 策略-Autonomous Swing Trader V1 (Alpaca-Paper)

on:
  # 定时触发已禁用，仅支持手动触发
  # schedule:
  #   # 美东时间下午 15:00 执行（周一到周五）
  #   # UTC 19:00 对应 EDT 15:00（夏令时，3月-11月）
  #   # UTC 19:00 对应 EST 14:00（冬令时，11月-3月）
  #   - cron: '01 19 * * 1-5'

  workflow_dispatch:
    inputs:
      autonomous_provider:
        description: 'LLM Provider'
        required: false
        type: choice
        default: 'gemini'
        options:
          - gemini
          - openai
          - openrouter
          - nvidia
      autonomous_model:
        description: 'LLM Model name (leave empty to use default for selected provider)'
        required: false
        default: 'gemini-3.6-flash'
      analysis_provider:
        description: 'Analysis Provider'
        required: false
        type: choice
        default: 'gemini'
        options:
          - openai
          - gemini
          - openrouter
          - nvidia
      analysis_model:
        description: 'Analysis Model name'
        required: false
        default: 'gemini-3.5-flash-lite'
      telegram_chat_id:
        description: 'Telegram Chat ID'
        required: false
        default: '-1003444893572'
      dry_run:
        description: 'Dry Run Mode (true/false)'
        required: false
        default: 'true'

jobs:
  run_autonomous_swing_trader_v1_alpaca:
    runs-on: ubuntu-22.04
    timeout-minutes: 50
    
    steps:
      - name: Set LLM configuration based on provider
        id: set_llm_config
        run: |
          PROVIDER="${{ github.event.inputs.autonomous_provider || 'gemini' }}"
          CUSTOM_MODEL="${{ github.event.inputs.autonomous_model }}"
          
          # 根据提供商设置默认模型
          case "$PROVIDER" in
            openai)
              DEFAULT_MODEL="gpt-5.4-mini"
              ;;
            gemini)
              DEFAULT_MODEL="gemini-3.6-flash"
              ;;
            openrouter)
              DEFAULT_MODEL="openai/gpt-4o-mini"
              ;;
            nvidia)
              DEFAULT_MODEL="z-ai/glm-5.2"
              ;;
            *)
              DEFAULT_MODEL="gemini-3.6-flash"
              ;;
          esac
          
          # 如果用户提供了自定义模型，使用自定义模型；否则使用默认模型
          if [ -n "$CUSTOM_MODEL" ]; then
            MODEL="$CUSTOM_MODEL"
          else
            MODEL="$DEFAULT_MODEL"
          fi
          
          echo "provider=$PROVIDER" >> $GITHUB_OUTPUT
          echo "model=$MODEL" >> $GITHUB_OUTPUT
          echo "Using provider: $PROVIDER, model: $MODEL"
      
      - name: Run autonomous swing trader v1 strategy with Alpaca
        run: |
          docker pull ghcr.io/yjq001/ibkr:latest
          
          docker run --rm --network host \
            --name autonomous_swing_trader_v1_alpaca \
            -e USE_MOCK_BROKER=false \
            -e BROKER_TYPE=alpaca \
            -e ALPACA_API_KEY="${{ secrets.ALPACA_API_KEY_PAPER_SHORT }}" \
            -e ALPACA_API_SECRET="${{ secrets.ALPACA_API_SECRET_PAPER_SHORT }}" \
            -e ALPACA_BASE_URL="https://paper-api.alpaca.markets" \
            -e DRY_RUN="${{ github.event.inputs.dry_run || 'false' }}" \
            -e STRATEGY_NAME="autonomous_swing_trader_v1" \
            -e STRATEGY_INSTANCE_ID="autonomous_swing_trader_v1_alpaca_paper" \
            -e OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
            -e OPENAI_MODEL="gpt-4o-mini" \
            -e GEMINI_API_KEY="${{ secrets.GEMINI_API_KEY }}" \
            -e OPENROUTER_API_KEY="${{ secrets.OPENROUTER_API_KEY }}" \
            -e NVIDIA_API_KEY="${{ secrets.NVIDIA_API_KEY }}" \
            -e OPENROUTER_HTTP_REFERER="https://github.com/easyllms/ibkr-bot" \
            -e OPENROUTER_APP_NAME="IBKR Bot" \
            -e AUTONOMOUS_PROVIDER="${{ steps.set_llm_config.outputs.provider || 'gemini' }}" \
            -e AUTONOMOUS_MODEL="${{ steps.set_llm_config.outputs.model || 'gemini-3.6-flash' }}" \
            -e ANALYSIS_PROVIDER="${{ github.event.inputs.analysis_provider || 'gemini' }}" \
            -e ANALYSIS_MODEL="${{ github.event.inputs.analysis_model || 'gemini-3.5-flash-lite' }}" \
            -e LLM_STREAMING=true \
            -e STRATEGIST_PROVIDER="openai" \
            -e STRATEGIST_MODEL="o4-mini" \
            -e MCP_SERVER_URL="${{ secrets.MCP_SERVER_URL || '' }}" \
            -e UPSTASH_REDIS_URL="https://smashing-spaniel-32228.upstash.io" \
            -e UPSTASH_REDIS_TOKEN="${{ secrets.UPSTASH_REDIS_TOKEN }}" \
            -e TELEGRAM_BOT_TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}" \
            -e TELEGRAM_CHAT_ID="${{ github.event.inputs.telegram_chat_id || '-1003444893572' }}" \
            -e TELEGRAM_NOTIFY_ENABLED=false \
            -e TELEGRAM_NOTIFY_URL="https://code.easyllms.com/api/telegram/send" \
            -e TELEGRAM_TIMEOUT_S=10 \
            -e APPROVAL_REQUIRED=false \
            -e APPROVAL_TIMEOUT_MINUTES=30 \
            -e APPROVAL_POLL_INTERVAL_SECONDS=60 \
            -e RISK_MAX_ITERATIONS=5 \
            -e RISK_MAX_REVISION_ROUNDS=5 \
            -e EXECUTOR_PRICE_CAP_BUFFER=2 \
            -e MEMORY_TRADE_HISTORY_DAYS=30 \
            -e MEMORY_CONVERSATION_ROUNDS=10 \
            -e MEMORY_INSIGHT_TTL_DAYS=7 \
            -e TRADING_TIMEZONE="America/New_York" \
            -e TRADING_RTH_ONLY=true \
            -e TRADING_START_HHMM="09:35" \
            -e TRADING_END_HHMM="15:55" \
            -e MIN_TRADE_VALUE_USD=50 \
            -e MIN_POSITIONS=2 \
            -e MAX_POSITIONS=8 \
            -e SPEC_MAX_WEIGHT_PCT=10 \
            -e CORE_MAX_WEIGHT_PCT=15 \
            -e MAX_POSITION_WEIGHT=0.15 \
            -e SLIPPAGE_BPS=15 \
            -e DIARY_TITLE="AI美股短线掘金" \
            -e INITIAL_CAPITAL="100000" \
            -e PORTFOLIO_CORE_MIN_PCT="50" \
            -e MAX_TOTAL_POSITION_PCT=120 \
            -e CORE_STOP_LOSS_PCT=8 \
            -e CORE_TAKE_PROFIT_PCT=15 \
            -e SPEC_STOP_LOSS_PCT=5 \
            -e SPEC_TAKE_PROFIT_PCT=20 \
            -e PROD_CODE="" \
            -e WECHAT_COVER_IMAGE_URL="https://cos.easyllms.cn/others/short.png" \
            ghcr.io/yjq001/ibkr:latest ibkr-bot run-once
      
      - name: Send Telegram notification
        if: always()
        run: |
          # 判断工作流状态
          if [ "${{ job.status }}" == "success" ]; then
            STATUS_EMOJI="✅"
            STATUS_TEXT="成功"
          else
            STATUS_EMOJI="❌"
            STATUS_TEXT="失败"
          fi
          
          # 获取工作流信息
          WORKFLOW_NAME="${{ github.workflow }}"
          RUN_NUMBER="${{ github.run_number }}"
          RUN_ID="${{ github.run_id }}"
          ACTOR="${{ github.actor }}"
          REPO="${{ github.repository }}"
          PROVIDER="${{ steps.set_llm_config.outputs.provider || 'gemini' }}"
          MODEL="${{ steps.set_llm_config.outputs.model || 'gemini-3.1-flash-lite' }}"
          DRY_RUN="${{ github.event.inputs.dry_run || 'false' }}"
          CHAT_ID="${{ github.event.inputs.telegram_chat_id || '-1003444893572' }}"
          
          # 构建消息内容
          MESSAGE=$(cat <<EOF
          ${STATUS_EMOJI} *工作流执行${STATUS_TEXT}*

          📋 *工作流信息*
          • 名称: ${WORKFLOW_NAME}
          • 运行编号: #${RUN_NUMBER}
          • 触发者: ${ACTOR}
          • 仓库: ${REPO}

          🤖 *策略配置*
          • 提供商: ${PROVIDER}
          • 模型: ${MODEL}
          • 模拟模式: ${DRY_RUN}
          • Broker: Alpaca

          🔗 *查看详情*
          [查看运行日志](https://github.com/${REPO}/actions/runs/${RUN_ID})
          EOF
          )
          
          # 使用 Python 构建 JSON（安全处理特殊字符）
          export MESSAGE CHAT_ID
          python3 <<'PYTHON_SCRIPT'
          import json
          import os
          
          message = os.environ.get('MESSAGE', '')
          chat_id = os.environ.get('CHAT_ID', '')
          
          payload = {
              "chat_id": chat_id,
              "text": message,
              "parse_mode": "Markdown",
              "disable_web_page_preview": True
          }
          
          with open("/tmp/telegram_message.json", "w", encoding="utf-8") as f:
              json.dump(payload, f, ensure_ascii=False, indent=2)
          PYTHON_SCRIPT
          
          # 发送 Telegram 消息
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d @/tmp/telegram_message.json \
            --fail --show-error || echo "Telegram notification failed, but continuing workflow"
